name: Update README cards

on:
  schedule:
    - cron: "0 0 * * *" # Runs once daily at midnight
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6

      - name: Ensure profile directory exists
        run: mkdir -p profile

      - name: Download cards
        shell: bash
        run: |
          download_keep_old() {
            local url="$1"
            local out="$2"
            local tmp="${out}.tmp"

            if curl -fL --retry 5 --retry-delay 10 --retry-all-errors -o "$tmp" "$url"; then
              mv "$tmp" "$out"
              echo "Updated $out"
            else
              rm -f "$tmp"
              echo "Download failed for $out, keeping existing file"
            fi
          }

          download_keep_old "https://github-readme-stats.vercel.app/api?username=${{ github.repository_owner }}&show_icons=true" "profile/stats.svg"
          sleep 5
          download_keep_old "https://github-readme-stats.vercel.app/api/top-langs?username=${{ github.repository_owner }}&layout=compact" "profile/top-langs.svg"
          sleep 5
          download_keep_old "https://github-readme-stats.vercel.app/api/pin?username=readme-tools&repo=github-readme-stats" "profile/pin-readme-tools-github-readme-stats.svg"

      - name: Commit cards
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f profile/streak.svg
          git add -A profile
          git diff --cached --quiet && exit 0
          git commit -m "Update README cards"
          git push